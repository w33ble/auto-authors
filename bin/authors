#!/usr/bin/env node
const { githubAuthors } = require('../');
const compileTemplate = require('../src/template');

githubAuthors().then(a => compileTemplate('compact', { authors: a }))
.then(out => console.log(out))
